import util from "util";

const name = "echo";
const name1 = "echo1"

console.info(`hello ${name} ${name1}`);

const person = {
    name: "echoo",
    name1: "echo1"
};

console.info(`Person : ${JSON.stringify(person)}`);
